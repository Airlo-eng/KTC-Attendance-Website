function ResetForm() {

    //TODO: Add link to main page. Add options for resetting payments (by year group, by school, by note). Add search bar for resetting (appear if paid value is not equal to yes).

    return (

        <div>

            Reset Form

        </div>

    );

}

export default ResetForm;